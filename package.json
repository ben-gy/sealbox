{
  "name": "sealbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Lock any file with a password, right in your browser — nothing ever uploaded.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.1.0",
    "vitest": "^4.1.9",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
